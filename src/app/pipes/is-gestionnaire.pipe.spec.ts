import { IsGestionnairePipe } from './is-gestionnaire.pipe';

describe('IsGestionnairePipe', () => {
  it('create an instance', () => {
    const pipe = new IsGestionnairePipe();
    expect(pipe).toBeTruthy();
  });
});
