import { IsUtilisateurPipe } from './is-utilisateur.pipe';

describe('IsUtilisateurPipe', () => {
  it('create an instance', () => {
    const pipe = new IsUtilisateurPipe();
    expect(pipe).toBeTruthy();
  });
});
